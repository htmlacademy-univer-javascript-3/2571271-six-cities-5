export enum AppRoutes {
  Root = '/',
  Login = '/login',
  Favorites = 'favorites',
  Offer = 'offer/:id',
}

export enum AuthStatus {
  Auth = 'AUTH',
  NotAuth = 'NOT_AUTH',
  Unknown = 'UNKNOWN',
}
